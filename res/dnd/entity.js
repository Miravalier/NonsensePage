export default class Entity {
    attributes

    get_attrs(name, callback) {
    }

    set_attrs(name, value) {
        send_object();
    }

    constructor() {
        console.log(this.constructor.name);
    }
}
